import { SombraNaTabelaDirective } from './sombra-na-tabela.directive';

describe('SombraNaTabelaDirective', () => {
  it('should create an instance', () => {
    const directive = new SombraNaTabelaDirective();
    expect(directive).toBeTruthy();
  });
});
